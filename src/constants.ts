export const ADDON_ID = 'storybook-addon-themes-webcomponent';
export const PARAM_KEY = 'themes';

export const CHANGE = `${ADDON_ID}/change`;
export const DECORATOR = `${ADDON_ID}/decorator`;
export const THEME = `${ADDON_ID}/theme`;
